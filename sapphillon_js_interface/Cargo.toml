[package]
name    = "sapphillon_js_interface"
version = "0.1.0"
edition = "2021"

# Stable ABI boundary crate — no JS engine dependency.
# Both sapphillon_js (provider) and sapphillon_core (consumer) depend on this.

[dependencies]
abi_stable = { workspace = true }
